{
  "name": "elk-dynamic-input-defaults",
  "version": "1.0.0",
  "description": "This action allows specifying defaults for dynamic input values",
  "main": "index.js",
  "author": "Pedro Burgos",
  "dependencies": {
    "@actions/core": "1.10.1",
    "@vercel/ncc": "^0.38.1"
  },
  "scripts": {
    "compile": "rm -rf dist && ncc build index.js -o dist"
  }
}
